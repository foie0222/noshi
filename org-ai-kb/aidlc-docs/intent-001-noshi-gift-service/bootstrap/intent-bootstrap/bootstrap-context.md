# Bootstrap Context — noshi-gift-service

## Classification

**greenfield**

Rationale: 対象リポジトリ `noshi` は空(`.git` のみ）で既存コードが存在しない。新規にゼロから構築するため greenfield に分類する。

## Repos in scope

none（新規構築。既存リポジトリの取り込みなし）

## RE-kb status

n/a（greenfield のため reverse-engineering 知識ベースは不要）

## Reverse-engineering

not needed（既存システムが無いため reverse-engineering ステージはスキップ）
