# Workflow Composition (auto, intent-009)
[Answer]: lean（requirements-analysis → user-stories → functional-design(--unit) → code-generation(--unit, CDK 実装)）。RE/wireframes/nfr/infra スキップ（infrastructure-design は intent-002 を参照）。OWASP 有効。
