# Domain Entities — noshi relationship balance（intent-006）
新規エンティティなし。派生のみ:
- party_balance(user): [{ party_name, received, given, diff, last_at, status, attention }]（internal、保存しない・本人スコープ集計）。
分類・本人スコープ不変（A01）。
