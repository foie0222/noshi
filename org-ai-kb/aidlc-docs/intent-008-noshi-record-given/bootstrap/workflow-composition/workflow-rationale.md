# Workflow Rationale (lean brownfield, intent-008)
requirements/user-stories/functional-design(--unit)/code-generation(--unit) を実行。RE/wireframes/nfr/infra スキップ（stack・設計不変、bug fix+小機能）。build-and-test は v2未実装。owasp 有効。
