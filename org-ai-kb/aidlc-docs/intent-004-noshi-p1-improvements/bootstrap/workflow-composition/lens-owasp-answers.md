# OWASP Lens — Answers (intent-004)
intent-003 を継承。第三者PII（氏名・関係・金額）を扱う。本人スコープ(A01)・入力検証(A03)・汎用エラー・監査(A09)・restricted非ログ・外部送信最小化を維持。
P1-2 は本人スコープの「可視化（felt trust）」であり、実体の認可は変更しない（表示・コピーの追加）。P1-3 集計は本人データのみ（A01）。
