# Workflow Rationale (lean, intent-009 IaC)
requirements/user-stories/functional-design(--unit)/code-generation(--unit, CDK) を実行。infrastructure-design は intent-002 を再利用（再実行しない）。RE/wireframes/nfr/infra-design/build-and-test スキップ。owasp 有効（最小権限・暗号化をコード化）。
