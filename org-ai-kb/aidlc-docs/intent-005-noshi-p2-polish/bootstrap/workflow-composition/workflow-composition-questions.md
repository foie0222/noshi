# Workflow Composition (auto, intent-005)
[ステージ]: lean（requirements-analysis → user-stories → functional-design(--unit) → code-generation(--unit)）。RE/wireframes/nfr/infra スキップ。
[ユニット]: noshi-service
[OWASP]: 有効（PII継続。P2 は主にUIだが本人スコープ不変）
