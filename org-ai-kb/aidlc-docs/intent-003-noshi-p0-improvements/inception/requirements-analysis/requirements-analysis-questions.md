# Requirements Analysis — Clarification（intent-003 P0 改善）

入力: design-review 001 / 既存 requirements・コード。P0-1〜3 ＋ given除外。

### Q1: お返し期限の標準ルール（P0-1 の核）

受領日（occurred_at）からの標準期限の初期値（上書き可）:

| 用途 | 標準期限 |
|---|---|
| 香典（香典返し） | 四十九日後（受領+49日）|
| 出産祝い（出産内祝い） | 受領+1ヶ月 |
| 結婚祝い（結婚内祝い） | 受領+1ヶ月 |
| 快気祝い | 受領+1ヶ月 |
| 一般慶事（入学・新築等） | 受領+1ヶ月 |
| お中元・お歳暮 | お返し不要（期限なし・礼状で対応）|

a) この初期ルールで実装（残日数表示・期限順ソート・超過は強調）
b) 調整したい

**Recommendation:** a)

[Answer]: a) 推奨初期値で実装（残日数・期限順・超過強調）

### Q2: 撮影レビュー体験（P0-2）

a) 「要所だけ確認」UIに集中（高信頼=確定済み表示、低信頼のみ強調）。まとめ撮り（バッチ）は次 intent。
b) まとめ撮り（イベント単位の複数レコード）も今回含める

**Trade Offs:** バッチは価値大だが工数も大。まず確認体験の魔法を取り戻すのが P0。

**Recommendation:** a) 今回は確認体験に集中。

[Answer]: a) 「要所だけ確認」に集中（バッチは次 intent）

### Q3: 収支フレーミング（P0-3）

a) ホームの主役を「お返し期限」に。収支の差分は前面から外す（必要なら控えめにサマリ内）。「お返しが必要/完了」を主軸に。
b) 差分表示を完全撤去
c) 現状維持に近い軽微調整

**Recommendation:** a)

[Answer]: b) 差分表示を完全撤去（収支・差分は見せない）

### Q4: given（あげた）の扱い（負債#1）

a) given は「未完了お返し」イベントを作らない（台帳には残るが、お返し対象外）。received のみ pending。
b) 既存のまま（given も pending に出す）

**Recommendation:** a) received のみお返し対象。

[Answer]: a) received のみお返し対象（given は台帳のみ・pending を作らない）

### Q5: スコープ確認

[Answer]: P0-1/2/3 ＋ given除外のみ。P1（弔事トーン/信頼可視化/贈与税）と P2 は次 intent。stack 不変（Python/React/DynamoDB、InMemory+モックでローカル動作）。
