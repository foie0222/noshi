# 依存は全て公開 npm レジストリから取得する。
# private リポジトリでは Dependabot が GitHub Packages(npm.pkg.github.com) の認証を
# 自動注入し、.npmrc が無いと "Private npm registries require ... a .npmrc file" で
# 更新/リベースが失敗する。明示的に公開レジストリを指定して解消する（挙動は既定と同じ）。
registry=https://registry.npmjs.org/
