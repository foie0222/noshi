-r requirements.txt
pytest==9.0.3
httpx==0.28.1
moto[dynamodb]==5.2.2  # DynamoRepository をモックでテスト
ruff==0.15.16          # lint + format
mypy==2.1.0            # 型チェック（strict）
pre-commit==4.6.0      # コミット前フック（ruff/mypy/biome）
